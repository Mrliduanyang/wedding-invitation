# 💒 婚礼导航系统

一个基于 Three.js 的 3D 交互式婚礼导航系统，让宾客以有趣的方式了解婚礼相关地点信息。

![Wedding Navigation System](https://img.shields.io/badge/Three.js-black?style=flat-square&logo=three.js&logoColor=white)
![Vite](https://img.shields.io/badge/Vite-646CFF?style=flat-square&logo=vite&logoColor=white)
![JavaScript](https://img.shields.io/badge/JavaScript-F7DF1E?style=flat-square&logo=javascript&logoColor=black)

## ✨ 特性

### 🎮 交互式 3D 导航
- **智能寻路系统**：基于改进的 A* 算法在城市道路网格上生成最优路径
- **平滑相机跟随**：电影级的相机跟随效果，使用线性插值实现丝滑过渡
- **动态速度控制**：转弯时自动减速，避免过冲和方向抖动
- **预测性转向**：提前看向前方路径点，实现自然的转向效果

### 🚗 双重出行模式
- **🚖 打车模式**：自动导航，小车沿路径自动行驶
- **🚗 自驾模式**：使用方向键（↑↓←→）手动控制小车

### 🏙️ 3D 城市场景
- **6×6 城市网格**：包含道路、建筑物、路灯等元素
- **法拉利 458 Italia 模型**：使用 Three.js 官方示例模型
- **动态光照效果**：方向光、环境光和建筑顶部发光指示器
- **飘浮气球装饰**：营造节日氛围

### 🎯 三个目的地
1. **💒 婚礼现场** - 南阳市梅溪国际酒店
   - 粉金渐变主题
   - LED 显示百年好合倒计时
   - 新人合照展示区

2. **🤵 新郎家**
   - 蓝色渐变主题
   - 浪漫爱情故事展示
   - 新郎个人照片区

3. **👰 新娘家**
   - 粉色渐变主题
   - 温馨回忆展示
   - 新娘个人照片区

### 🎨 精美 UI 设计
- **LED 滚动屏幕**：无缝循环滚动的霓虹灯文字效果
- **玻璃态弹窗**：现代化的毛玻璃效果和渐变背景
- **平滑动画**：所有交互都有流畅的过渡动画
- **响应式布局**：适配不同屏幕尺寸

### 🎯 导航线特效
- **动态绘制**：路线预览时的动画绘制效果
- **平滑消失**：小车行驶过的路段自动渐变消失
- **自定义 Shader**：使用 WebGL Shader 实现精确的透明度控制

## 🚀 快速开始

### 环境要求
- Node.js >= 14.0.0
- npm 或 yarn

### 安装依赖
```bash
npm install
```

### 开发模式
```bash
npm run dev
```

访问 `http://localhost:5173` 查看效果

### 构建生产版本
```bash
npm run build
```

### 预览生产版本
```bash
npm run preview
```

## 🎮 使用说明

1. **选择出行方式**：点击"打车"或"自驾"按钮
2. **选择目的地**：从下拉菜单选择婚礼现场、新郎家或新娘家
3. **生成路线**：点击"生成导航路线"按钮查看路径预览
4. **开始导航**：点击"开始导航"按钮
   - 打车模式：小车自动行驶
   - 自驾模式：使用方向键控制
5. **到达目的地**：弹出目的地详情弹窗

### 相机控制
- **鼠标拖拽**：旋转视角（导航开始前）
- **滚轮**：缩放视角
- **右键拖拽**：平移视角

## 🛠️ 技术栈

### 核心技术
- **Three.js** - 3D 渲染引擎
- **Vite** - 现代化的前端构建工具
- **Vanilla JavaScript** - 原生 JavaScript，无框架依赖

### 关键技术实现

#### 1. 平滑转向算法
```javascript
// 预测性转向：提前看向前方路径点
const lookAheadDistance = carSpeed * 12;
// 动态速度控制：根据转向角度调整速度
const speedFactor = 1 - Math.min(angleDegrees / 180, 0.7);
```

#### 2. 相机跟随系统
```javascript
// 线性插值实现平滑过渡
const cameraLerpFactor = 0.12;
camera.position.x += (targetPos.x - camera.position.x) * cameraLerpFactor;
```

#### 3. 导航线消失效果
```javascript
// 使用自定义 Shader Material
const shaderMaterial = new THREE.ShaderMaterial({
  vertexShader: `
    attribute float alpha;
    varying float vAlpha;
    void main() {
      vAlpha = alpha;
      gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);
    }
  `,
  fragmentShader: `
    uniform vec3 color;
    uniform float opacity;
    varying float vAlpha;
    void main() {
      gl_FragColor = vec4(color, opacity * vAlpha);
    }
  `
});
```

## 📁 项目结构

```
wedding-nav/
├── src/
│   ├── main.js          # 主逻辑：3D场景、导航系统
│   └── style.css        # 样式：UI组件、动画效果
├── public/
│   └── ferrari.glb      # 法拉利458 3D模型
├── index.html           # HTML入口：UI结构
├── package.json         # 依赖配置
└── README.md           # 项目文档
```

## 🎨 自定义配置

### 修改目的地位置
在 `src/main.js` 中找到 `createBuildings()` 函数：
```javascript
const positions = [
  {
    x: -150,
    z: -150,
    name: '婚礼现场',
    destination: 'wedding',
    // ... 其他配置
  }
];
```

### 修改弹窗主题色
在 `src/style.css` 中修改渐变色：
```css
#weddingModal .modal-content {
  background: linear-gradient(135deg, #ff6b9d 0%, #ffa07a 50%, #ffd700 100%);
}
```

### 修改LED文字
在 `index.html` 中修改 LED 内容：
```html
<span class="led-content">💒 你的自定义文字 💒</span>
```

## 🐛 已知问题

- 在低性能设备上可能出现帧率下降
- 某些移动设备上触摸控制体验待优化

## 📝 开发计划

- [ ] 添加更多车辆模型选择
- [ ] 支持上传自定义照片
- [ ] 添加背景音乐
- [ ] 优化移动端体验
- [ ] 添加多语言支持

## 📄 许可证

MIT License

## 🙏 致谢

- Three.js 团队提供的优秀 3D 引擎
- 法拉利 458 Italia 模型来自 Three.js 官方示例

## 👨‍💻 作者

欢迎提交 Issue 和 Pull Request！

---

**⭐ 如果这个项目对你有帮助，请给个 Star 支持一下！**

